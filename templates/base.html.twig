<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width" />
        <title>{% block title %}Panel Wichisoft - Restaurants{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('build/css/bootstrap.min.css') }}"/>
            <link rel="stylesheet" href="{{ asset('build/css/font-awesome/4.2.0/css/font-awesome.min.css') }}"/>
            <link rel="stylesheet" href="{{ asset('build/css/main.css') }}"/>
            <style>
                .navbar-default .navbar-nav>.open>a, .navbar-default .navbar-nav>.open>a:hover, .navbar-default .navbar-nav>.open>a:focus {
                    background-color: transparent;
                    color: #555;
                }
                .user-mini>span, .panel-menu .dropdown-menu>li>a {
                    color: white;
                }
                li.dropdown.open {
                    background-color: rgba(0,0,0,.2);
                }

                table {
                    font-family: "Trebuchet MS", Arial, Helvetica, sans-serif;
                    border-collapse: collapse;
                    width: 100%;
                    margin-bottom: 20px;
                    border: 1px solid #f2f2f2;
                }

                td, th {
                    border: 1px solid #ddd;
                    padding: 8px;
                }

                td.left {
                    text-align: left;
                }
                td.center {
                    text-align: center;
                }
                td.restaurant-link {
                    word-wrap: break-word;
                }
                tr:nth-child(even){background-color: #f2f2f2;}

                tr:hover {background-color: #ddd;}

                th {
                    padding-top: 12px;
                    padding-bottom: 12px;
                    text-align: center;
                    background-color: #505559;
                    color: white;
                }

                p {
                    font-size: 18px;
                }
                img.icon {
                    height: 25px;
                }
                .buttons {
                    margin-bottom: 15px;
                }
                button.addItem {
                    padding: 5px 10px;
                }
                .pagename {
                    margin-bottom: 15px;
                }
                td {
                    border: none;
                    text-align: center;
                }

                a { text-decoration: none !important; }

                .modal-open {margin:0 !important;}

                th { text-align: center; }

                .section-title {
                    color: #413E3E;
                    font-size: 24px;
                    font-weight: bold;
                    border-bottom: 1px solid #CCCCCC;
                    margin-bottom: 15px;
                }

                .title-page {
                    color: #413E3E;
                    text-align: left;
                    font-weight: bolder;
                    font-size: 20px;
                    margin-bottom: 5px;
                }

                .description {
                    font-size: 20px;
                    margin: 0px 15px 25px 15px;
                    color: #525252;
                    /*padding-left: 50px;*/
                    padding: 5px;
                }

                .description-border {
                    border: 1px solid #cccccc;
                    border-radius: 2px;
                }

                @media (min-width: 767px) {
                    .description-border {
                        width: 98%;
                    }
                }
                .pad-lt-15 {padding-left: 15px;} .pad-bt-15 {padding-bottom: 15px;}

                .font-20 { font-size: 20px; }

                .border {
                    border: 1px solid #cccccc;
                    border-radius: 5px;
                    margin: 0px;
                    padding 5px;
                }

                .mb-10 { margin-bottom: 10px; }

                .elements table, .elements td, .elements th {
                    padding: 2px;
                    border: 1px solid #cccccc;
                    border-radius: 5px;

                }
                .elements td.actions {
                    text-align: center;
                }
                p {
                    font-size: 18px;
                }
                .pagename {
                    margin-bottom: 15px;
                }

                .img-profile {
                    width: 200px;
                    margin: 10px 0px 10px 0px;
                }

                img.qr-code {
                    width: 50px;
                    margin: 15px 0px 0px 0px;
                }

                img.restaurant-logo {
                    max-height: 50px;
                    margin: 15px 0px 0px 0px;
                }
                a { text-decoration: none !important; }

                .section-title {
                    color: #413E3E;
                    font-size: 24px;
                    font-weight: bold;
                    border-bottom: 1px solid #CCCCCC;
                    margin-bottom: 15px;
                }

                .pull-right {
                    margin-right: 15px;
                }
                .pad-lt-15 {padding-left: 15px;} .pad-bt-15 {padding-bottom: 15px;}

                .action-buttons {
                    display: inline-block;
                    white-space: nowrap;
                    margin: auto;
                    height: 100%;
                    vertical-align: middle;
                }

            </style>
        {% endblock %}
    </head>
    <body>
        {% include('header.html.twig') %}
        {% block body %} {% endblock %}

        {% block javascripts %}
            <!-- Include the compiled script-->
            <script type="text/javascript" src="{{ asset('build/js/jquery.min.js') }}"></script>
            <script type="text/javascript" src="{{ asset('./build/app.js') }}"></script>
            <script type="text/javascript" src="{{ asset('build/js/bootstrap.min.js') }}"></script>
            <script type="text/javascript" src="{{ asset('build/js/header.js') }}"></script>
        {% endblock %}
    </body>
</html>
