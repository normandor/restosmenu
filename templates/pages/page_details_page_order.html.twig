{% extends 'base.html.twig' %}

{% block stylesheets %}
  {{ parent() }}
{% endblock %}

{% block body %}
  <div id="main" class="container">
    <div class="row">
      <div id="sidebar-left" class="col-xs-2 col-sm-2">
        {{
          render(
            controller(
              'App\\Controller\\MenuController::index',
              {
                keep_open: 0,
                route: route
              },
              {
                username: user,
                avatar: true
              }
            )
          )
        }}
      </div>

      <div id="content" class="col-xs-12 col-sm-10">
        <div class="row">
          <div id="breadcrumb" class="col-xs-12">
            <a href="#" class="show-sidebar"><i class="fa fa-bars"></i></a>
            <ol class="breadcrumbpanel pull-left">
              <a href="#">Restaurant</a>
            </ol>
          </div>
        </div>

        <div class="pagename row pad-lt-15 pad-bt-15">
          <div class="col-sm">
            <h1>
              {{ pageName }}
              <br />
            </h1>
          </div>
        </div>

          <div class="row font-20" style="margin-bottom:15px">
              <div class="col-sm-10 mb-10">
                  <h2>Orden de las categorias e imagenes</h2>
              </div>
              <div class="col-sm-10 mb-10">
                  <table>
                      <tr>
                          <th>
                              Item
                          </th>
                          <th>
                              Posicion
                          </th>
                          <th>
                              Visible
                          </th>
                          <th></th>
                      </tr>
                      {% for setting in settings %}
                          <tr>
                              <td class="left">
                                  {{ setting.name }}
                              </td>
                              <td class="left">
                                  <span style="font-family: {{ setting.font_family }};">{{ setting.font_family }}</span>
                              </td>
                              <td class="left">
                                  {{ setting.font_size }}
                              </td>
                              <td class="actions">
                                  <div class="buttons action-buttons">
                                      <img class="icon" src="../images/icono_edit.png"
                                           onclick="showEditStyle(1);"
                                           style="cursor: pointer;" />

{#                                      #}{#                                      <img class="icon"#}
{#                                      #}{#                                           src="../images/icono_delete.png"#}
{#                                      #}{#                                           onclick="showDeleteItem('{{ restaurant.id }}', '{{ restaurant.name|raw }}');"#}
{#                                      #}{#                                           style="cursor: pointer;" />#}
{#                                  </div>#}
{#                              </td>#}
                          </tr>

                      {% endfor %}
                  </table>
                  {#                  <button type="button" title="Agregar item"#}
                  {#                          class="btn btn-success btn-lg addItem"#}
                  {#                          onClick="showAddRestaurant();">#}
                  {#                      +#}
                  {#                  </button>#}
              </div>
              {#                  Restaurant seleccionado: <b>{{ selectedRestaurant }}</b>#}
          </div>
      </div>
    </div>
  </div>
    {{ include('user/modals/modal_confirm.twig') }}
    {{ include('user/modals/modal_general.twig') }}
{% endblock %}

{% block javascripts %}
  {{ parent() }}
  <script>
    function showAddRestaurant() {
        $('.modal-body-new-user').html('<div align="center"><img height="100" src="{{ asset('images/loading.gif') }}"></div>');
        $('.modal-body-new-user').load('../page/element/addRestaurant/', function () {});
        $('.modal-title').html('Nuevo restaurant');
        $("#newItemModal").modal('show');
    }

    function showDeleteItem(id, name) {
        $(".modal-header #sectionId").val(id);
        $('#myModalLabelMessage').html(`¿Confirma la eliminacion de ${name}?`);

        $("#confirmModal").modal('show');
    }

    function showEditRestaurant(id) {
        $('.modal-body-new-user').html('<div align="center"><img height="100" src="{{ asset('images/loading.gif') }}"></div>');
        $('.modal-body-new-user').load('../page/element/editRestaurant/'+id, function () {});
        $('.modal-title').html('Editar restaurant');
        $("#newItemModal").modal('show');
    }

    var modalConfirmDelete = function (callback) {
        $("#modal-del-btn-si").on("click", function () {
            callback(true);
            $("#confirmModal").modal('hide');
        });

        $("#modal-del-btn-no").on("click", function () {
            callback(false);
            $("#confirmModal").modal('hide');
        });
    };

    modalConfirmDelete(function (confirm) {
        if (confirm) {
            let sectionId = $("#sectionId").val();
            let path = '{{ path('remove_restaurant', {'id': 'sectionId'}) }}';
            let finalPath = path.replace("sectionId", sectionId);

            $.ajax({
                url: finalPath,
                type: 'POST',
                dataType: 'json',
                success: function (data) {
                    location.reload();
                },
                error: function (xhr, status) {
                    alert("{{ 'alert.error.processing' | trans }}");
                }
            });
        } else {

        }
    });
  </script>
{% endblock %}
